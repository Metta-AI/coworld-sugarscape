# Round-2 revision delta for sugarscape-v2-design HTML

Revise `/Users/jamesboggs/coding/coworlds/coworld-sugarscape/docs/designs/sugarscape-v2-design-2026-08-07.html`
**in place** to match the revised authority
`/Users/jamesboggs/coding/coworlds/coworld-sugarscape/docs/v2-design.md`
(read it in full first — every "revised 2026-08-10" marker is a change to
carry over). Do NOT touch the two DC-COMMENTS blocks. Keep all existing
markup patterns and design tokens. The masthead eyebrow becomes:
"Design Document · rev. 2026-08-10 · Status: Design complete — pre-implementation".

## Structural changes (James's review, verbatim requirements)

1. **Move design history out of the body.** The two "· Human override"
   decision records (E1 in §6, F1 in §7) leave the body. Create a new
   appendix section "Design History" (id `history`, after the decision
   index, before sources; add TOC entry) that holds: both override
   records (reframed as history — E1's body rationale is now the
   full-information argument, see below), and a short dated revision log
   (2026-08-07 initial 35 decisions; 2026-08-10 review round revisions +
   three discussion resolutions). The Alternatives section keeps its
   existing E1/F1 override mentions.
2. **Tighten Figure 4** (one-core-three-shells, §8): reduce dead space —
   smaller viewBox, tighter boxes/margins, same content and palette.

## Content changes (all sourced from the revised v2-design.md — verify each there)

- **Exec summary:** update the two-override sentence (overrides now live
  in Design History; E1's rationale is stated on its own terms). Add one
  sentence: the 2026-08-10 review made observation visibility fully
  configurable, adopted per-agent instance isolation (dropping the
  scratchpad), moved rating to score/EWMA, and parked a novel "speaking"
  mechanic as future work.
- **§2 seats:** seats/total_agents unbounded (was "up to 64"); flagship
  stays 64; classic 250-agent worlds legitimate. A2 random_parent:
  first child seeded 50/50, subsequent same-couple children via
  Thue–Morse.
- **§3 scoring:** new rating paragraph — ranked leagues use platform
  `ranking.algorithm="score"` with EWMA standing (no Elo, no platform
  modification); B6 tie-break retained but noted as less central.
- **§4 world table:** features row → note inheritance and
  drop_uninherited are separate flags.
- **§5 lifecycle (D4):** two independent flags, four meaningful combos,
  ranked default both on.
- **§6 timestep:**
  - E1 body rationale rewritten: sequential activation is integral —
    full information per action; per-tick reshuffle spreads first-mover
    benefit; Thue–Morse ordering is a candidate refinement (parked).
  - E4/E5: run-to-quiescence with cap semantics; caps default 8 (was
    2–3); per-cluster parallelization implementation note (disjoint
    von-Neumann clusters, per-cluster seeded streams, deterministic).
  - E2: hidden-holdings variant rule (see F1 below) cross-referenced.
  - E6: loans in either direction (offers AND requests);
    `collection_floor` flag (metabolism-protected collection);
    `shortfall_mode` enum table {write_off default, renew (classic-style,
    aggregate-ratio interest), sim_covers (mints lender whole —
    inflationary variant lever)}.
  - E7: initial tribes per-seat (seeded balanced seat→tribe assignment);
    declarable `tribeless` status variant (cross-tribe to everyone).
- **§7 policy boundary:**
  - F1 → per-attribute visibility CONFIG (table with ranked defaults:
    seat/holdings/tribe/sick visible; age/sex/fertile-now hidden;
    genes hidden; max_age never visible to others). Hidden-holdings
    physics: combat becomes always-legal-cross-tribe with unchanged
    resolution (strictly-richer wins, ties: attacker loses); trade
    `want` unclamped (voiding already covers unpayable acceptances);
    hidden sickness needs no rules. Own max_age visible to self by
    default, config to hide even from self.
  - F2 → REPLACED: per-agent component instance isolation
    (agent_isolation, ranked default on); P1 enforced under Arena;
    newborns blank; WS shell honest-rules; scratchpad DROPPED from
    protocol v1. Update the key-points bullet and the callout
    accordingly (the "honest-rules clause" callout becomes the
    isolation decision record).
  - A4/Time: "invocation" defined; new target-semantics + interim
    paragraph (freeze everywhere until Arena supports soft timeouts;
    pointer to docs/issues/arena-soft-timeout-limitation.md).
- **§9 determinism:** add the precision/binning analysis paragraph
  (32.32 lattice ≈ 2.3e-10; ~1e-9 relative welfare error vs ~1e-3..1e-4
  smallest gameplay delta — six orders of headroom; binning never
  threatens determinism; 96-bit (64.32) accumulator; published score
  quantized (e.g. 2^-20) with survivor bits below).
- **§11 risks:** update — instance count now tracks agents (sharper
  Arena scaling risk); timeout interim noted; blind-mating risk stays.
- **New body or appendix-adjacent section "Future Work"** (id `future`,
  TOC entry, before Alternatives or after Risks — your call for flow):
  the speaking mechanic as sketched in v2-design.md §11 (targeted
  E4-shaped speech, region = probably neighbors, quiescence rounds,
  possibly injected between all phases, cheap talk/deception legal,
  public in replays, blank-newborn culture-channel interplay), plus the
  parked items (Thue–Morse ordering, order-book/free-form variants,
  Arena llm import).
- **Alternatives section:** add round-2 rejected/superseded notes: fixed
  minimal visibility superseded by per-attribute config; scratchpad
  dropped (superseded by instance isolation); Elo rejected for
  score/EWMA; 2–3 round caps raised.
- **Decision index appendix:** update rows A1, A2 (Thue–Morse), B6
  (EWMA note), D4 (two flags), E1, E4, E5, E6, E7, F1, F2 to the revised
  resolutions, each marked "rev. 2026-08-10" where changed.
- **Sources:** add `docs/issues/arena-soft-timeout-limitation.md`.

## Verification the subagent must do before returning

- DC-COMMENTS blocks byte-identical to before the edit.
- All TOC hrefs (including new `history` and `future`) resolve.
- Grep-level sweep: "scratchpad" appears only in history/alternatives
  framing (dropped), "Elo" only in the rating rationale as the rejected
  option, no remaining "up to 64 seats" claims, no "default 2–3" round
  claims.
- Report section ids and anything from this delta you could not place.
