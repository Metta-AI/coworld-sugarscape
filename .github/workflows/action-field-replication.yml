name: Event Markov closure study

on:
  push:
    branches: [observatory-replication-20260815]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  replicate:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Compile study
        run: |
          python -m py_compile tools/replicate_action_field_observables.py
          python -m py_compile tools/replicate_event_markov_closure.py
      - name: Run 5-seed-per-regime event Markov closure study
        env:
          PYTHONHASHSEED: '0'
        run: python tools/replicate_event_markov_closure.py --seeds 5 --jobs 6 --output build/event-markov-closure.json
      - name: Upload study
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: event-markov-closure
          path: build/event-markov-closure.json
          if-no-files-found: warn
      - name: Persist study on experiment branch
        if: success()
        run: |
          mkdir -p experiment-results
          cp build/event-markov-closure.json experiment-results/event-markov-closure.json
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add experiment-results/event-markov-closure.json
          git commit -m 'Record event Markov closure results'
          git push
