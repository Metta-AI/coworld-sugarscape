name: Action-field replication study

on:
  push:
    branches: [observatory-replication-20260815]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  replicate:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Compile studies
        run: |
          python -m py_compile tools/replicate_action_field_observables.py
          python -m py_compile tools/replicate_discrete_lagrangian.py
          python -m py_compile tools/replicate_projective_lagrangian.py
      - name: Run 3-seed-per-regime projective Lagrangian pilot
        env:
          PYTHONHASHSEED: '0'
        run: python tools/replicate_projective_lagrangian.py --seeds 3 --jobs 6 --output build/projective-lagrangian-pilot.json
      - name: Upload pilot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: projective-lagrangian-pilot
          path: build/projective-lagrangian-pilot.json
          if-no-files-found: warn
      - name: Persist pilot on experiment branch
        if: success()
        run: |
          mkdir -p experiment-results
          cp build/projective-lagrangian-pilot.json experiment-results/projective-lagrangian-pilot.json
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add experiment-results/projective-lagrangian-pilot.json
          git commit -m 'Record projective Lagrangian pilot results'
          git push
