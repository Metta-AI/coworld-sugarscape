name: Action-field replication study

on:
  push:
    branches: [observatory-replication-20260815]
  workflow_dispatch:

jobs:
  replicate:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Compile study
        run: python -m py_compile tools/replicate_action_field_observables.py
      - name: Run canonical 30-seed replications
        env:
          PYTHONHASHSEED: '0'
        run: python tools/replicate_action_field_observables.py --seeds 30 --jobs 6 --output build/action-field-replication-study.json
      - name: Upload study
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: action-field-replication-study
          path: build/action-field-replication-study.json
          if-no-files-found: warn
